# LaPSEE Minicourse — pinned Python environment
# Install with: pip install -r requirements.txt
# Python 3.10 or newer is required (syllabus §5).

# Core numerical / data stack
# pandas pinned <2.2 because pandapower 2.14 writes back into result frames
# and pandas-2 copy-on-write makes those buffers read-only.
numpy>=1.26,<2.0
pandas>=2.1,<2.2
scipy>=1.11,<2.0
matplotlib>=3.8,<4.0

# Graphs and power systems
networkx>=3.2,<4.0
pandapower>=2.14.10,<3.0

# Spatial analysis (PySAL family)
libpysal>=4.9,<5.0
esda>=2.5,<3.0
spreg>=1.4,<2.0
splot>=1.1,<2.0

# Geospatial support (Session 2)
geopandas>=0.14,<2.0
shapely>=2.0,<3.0

# Auxiliary
scikit-learn>=1.3,<2.0
statsmodels>=0.14,<1.0
tqdm>=4.66,<5.0

# Agent-based modeling and MARL (Session 3)
mesa>=3.0,<4.0
pettingzoo>=1.24,<2.0
stable-baselines3>=2.3,<3.0

# Notebook tooling
jupyter>=1.0,<2.0
ipywidgets>=8.1,<9.0
